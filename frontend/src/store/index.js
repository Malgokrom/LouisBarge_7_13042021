import { createStore } from 'vuex';

export default createStore({
    state: {
        url_api: 'http://localhost:3000/api'
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    }
});
